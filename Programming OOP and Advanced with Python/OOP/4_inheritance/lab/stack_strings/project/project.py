# Create a class Stack that can store only strings and has
#  the following functionality:
# •	Instance attribute: data: list
# •	Method: push(element) – adds an element at the end
#  of the stack
# •	Method: pop() – removes and returns the last element 
# in the stack
# •	Method: top() - returns a reference to the topmost 
# element of the stack
# •	Method: is_empty() - returns boolean True/False
# •	Override the string method to return the stack data in the format: 
# "[{element(N)}, {element(N-1)} ... {element(0)}]"
#
class Stack:
    data: list
    def __init__(self):
        pass
    def push(self, element):
        data.push(element)
    def pop(self, element):
        data.pop(element)
    def top(self, element):
        the_top = data.top(element)
    def is_empty()(self, element):
        return is_empty()


# 
return f"[{element(N)}, {element(N-1)} ... {element(0)}]"